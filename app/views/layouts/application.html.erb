<!DOCTYPE html>
<html>
<head>
  <!-- meta-tags gem call-->
  <%= display_meta_tags %>
  <title>looklove - Reviews of Asian beauty products and skincare brands</title>
  <%= render 'layouts/bootstrap' %>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1">
</head>
<body>
<% title 'looklove, skincare product reviews' %>
<% description 'Get reviews and photos for Asian skincare and beauty brands' %>
<% keywords 'reviews, community, forum, posts, asian, skincare, beauty, products, lists, regimen' %>
<% nofollow %>
<% noindex %>
<div class = "meta-container">
<%= render 'layouts/navbar' %>
<div class="main-container">
  <div class="container-fluid">
      <% flash.each do |name, msg| %>
        <% if msg.is_a?(String) %>
          <div class="alert alert-<%= name == "notice" ? "success" : "danger" %> alert-dismissable">
            <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
            <%= content_tag :div, msg, :id => "flash_#{name}" %>
          </div>
        <% end %>
      <% end %> 
  <%= yield %>
  </div>
  </div>
</div>
<div class="footer-container">
<%= render 'layouts/footer' %>
</div>
</body>
</html>
