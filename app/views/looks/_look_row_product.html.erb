<div class="col-xs-4 look-row-product">
  <% if product.link.present? %>
    <%= link_to image_tag(product.image.small_thumb), product %>
  <% else %>
    <%= image_tag(product.image.small_thumb) %>
  <% end %>
</div>
