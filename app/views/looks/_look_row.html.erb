<div class="col-xs-12 look-row">
  <h3><%= link_to look.title, look %></h2>
  <% look.products.first(3).each do |product| %>
    <%= render partial: 'looks/look_row_product', locals: { product: product } %>
  <% end %>
</div>
