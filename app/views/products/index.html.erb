<% title 'stuff' %>
<% description 'stuff' %>
<% keywords 'stuff' %>

<div class="jumbotron">
  <h2>Walletthing is the easiest place to compare cryptocurrency wallets</h2>
  <!-- <h4>Research, review, and rate the newest digital coin wallets.</h4> -->
  <%= render 'products/login', locals: { product: @product } %>
</div>

  <!-- <div class="btn-group" role="group" aria-label="...">
    <button type="button" class="btn btn-default"><i class="fa fa-globe" aria-hidden="true"></i>&nbsp;&nbsp;Web</button>
    <button type="button" class="btn btn-default"><i class="fa fa-desktop" aria-hidden="true"></i>&nbsp;&nbsp;Desktop</button>
    <button type="button" class="btn btn-default"><i class="fa fa-mobile" aria-hidden="true"></i>&nbsp;&nbsp;Mobile</button>
    <button type="button" class="btn btn-default"><i class="fa fa-suitcase" aria-hidden="true"></i>&nbsp;&nbsp;Offline</button>
  </div> -->

</p>
      <ul class="list-group">
        <% Product.all.each do |product| %>
          <li class="list-group-item">
          <%= render partial: 'product_row', locals: { product: product } %>
      </li>
        <% end %>
      </ul>

<script>
mixpanel.track("Viewed Home");
<% if current_user %>
mixpanel.identify("<%= current_user.id %>");
mixpanel.people.set({
  "$email" : "<%= current_user.email %>",
  "name" : "<%= current_user.user_name %>",
  "User ID" : "<%= current_user.id %>",
  <% end %>
});
</script>

<script>
mixpanel.track_links(".feed-user a", "Clicked Feed", {"Item" : "User"})
mixpanel.track_links(".review-item a", "Clicked Feed", {"Item" : "Review"});
mixpanel.track_links(".use-item a", "Clicked Feed", {"Item" : "Use"});
mixpanel.track_links(".want-item a", "Clicked Feed", {"Item" : "Want"});
mixpanel.track_links(".post-item a", "Clicked Feed", {"Item" : "Post"});
</script>
