<%= form_for(@applicant) do |f| %>
  <% if @applicant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@applicant.errors.count, "error") %> prohibited this applicant from being saved:</h2>

      <ul>
      <% @applicant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :email %><br>
    <%= f.text_field :email, autofocus: true, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label 'What does "beauty" mean to you?' %><br>
    <%= f.text_area :info, class: "form-control" %>
  </div>
  <div class="actions">
    <%= f.submit 'Get on the list', class: "btn btn-primary btn-lg" %>
  </div>
<% end %>
