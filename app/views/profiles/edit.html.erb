<% nofollow %>
<% noindex %>

<div class="user-info">
	<h1><%= @user.user_name %></h1>
	<div class="user-avatar"><%= image_tag(@user.avatar.try(:image)) %></div>
</div>
<center>
<%= render 'form' %>

<%= link_to 'Cancel', user_path(@user) %>
</center>